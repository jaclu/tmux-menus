#!/bin/bash
# This is sourced. Fake bang-path to help editors and linters
# shellcheck disable=SC2034

skip_plugins=(
    # checkbashisms
    # flake8
    # markdownlint
    # shellcheck
    # vale
)
excludes+=(
)
prefixes+=(
    cache/
)
suffixes+=(
)

